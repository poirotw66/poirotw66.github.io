---
interface Props {
  eyebrow?: string;
  title?: string;
  text: string;
  buttonText: string;
  buttonHref: string;
  /** e.g. _blank for external links */
  buttonTarget?: string;
  buttonRel?: string;
  /** Optional anchor id for in-page hash / scroll spy */
  id?: string;
}

const { eyebrow, title, text, buttonText, buttonHref, buttonTarget, buttonRel, id } = Astro.props;
---

<div class:list={['cta-block', id ? 'home-scroll-anchor' : undefined]} id={id}>
  <div class="cta-block-copy">
    {eyebrow && <span class="cta-block-eyebrow">{eyebrow}</span>}
    {title && <h2>{title}</h2>}
  <p>{text}</p>
  </div>
  <a href={buttonHref} class="btn btn-primary" target={buttonTarget} rel={buttonRel}>{buttonText}</a>
</div>
